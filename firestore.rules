rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /responses/{docId} {
      // 누구나 설문 제출 가능
      allow create: if true;

      // 관리자 페이지에서 읽기·수정·삭제는 클라이언트 SDK로 처리
      // PIN 게이트는 클라이언트 사이드에서 적용됨
      allow read: if true;
    }
  }
}
