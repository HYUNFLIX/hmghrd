rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /responses/{docId} {
      // 누구나 설문 제출 가능
      allow create: if true;

      // 인증된 관리자만 읽기·수정·삭제 가능
      allow read, update, delete: if request.auth != null;
    }
  }
}
